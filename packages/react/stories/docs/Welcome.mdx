import { Meta } from "@storybook/blocks";

<Meta title="Introduction/Welcome" />

# Sebell Design System

A multi-brand design system that turns Figma decisions into a living UI library.

## What's in here

- **Components** — React building blocks (Button, Input, Stack, Text, Icon). Browse the **Components** section in the sidebar.
- **Tokens** — the design decisions (colors, typography, spacing, radius) that flow from Figma into code. The **Tokens** section is a live catalog — what you see is exactly what's currently built.
- **Guides** — how to update tokens from Figma, how to add components, how to consume the system in an app.

## Who it's for

- **Designers** working in a Sebell DS brand — to see what's available, how each component behaves under different brands and densities, and what changes when a token moves.
- **Engineers** building UIs — to install `@ds/react`, drop in components, and have brand/density theming and accessibility handled automatically.

## Two things to know about the toolbar

Every story has two switchers at the top — **Brand** (paintbrush icon) and **Density** (grow icon).

- **Brand** flips colors, fonts, and brand-specific component decisions (like Brand A's blue button vs Brand B's green one).
- **Density** flips spacing — components tighten in `compact`, breathe in `default`.

These two are independent. You can be in Brand A + Compact, or Brand B + Default, or any combination. The page re-renders instantly when you change either.

That toolbar is the heart of the system. Anything that visually changes between brands or densities should be doing so through tokens — never hardcoded values.

## Where to go next

- New here? Read [Architecture](?path=/docs/introduction-architecture--docs) to see how tokens flow from Figma into the running Storybook.
- Just changed something in Figma and want to know what happens next? [Updating from Figma](?path=/docs/guides-updating-from-figma--docs).
- Adding a new component? [Adding a Component](?path=/docs/guides-adding-a-component--docs).
- Consuming `@ds/react` in your own app? [Using Components](?path=/docs/guides-using-components--docs).
